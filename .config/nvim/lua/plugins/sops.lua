-- Конфигурация для работы с SOPS-зашифрованными файлами

return {
	"trixnz/sops.nvim",
	event = "BufRead", -- Загружать при чтении файла
	config = function()
		require("sops").setup({
			-- Автоматически расшифровывать при открытии
			enabled = true,

			-- Показывать сообщения о расшифровке/зашифровке
			debug = false,

			-- Автоматически определять SOPS файлы (по наличию 'sops:' в содержимом)
			-- Плагин сам проверит yaml/json файлы
		})
	end,
}
